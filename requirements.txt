# The analysis, NVD-join (from cache), and rendering pipeline in this repo is pure
# Python standard library (Python 3.8+). No third-party packages are required to
# reproduce every committed artifact from the provided corpus snapshot and caches:
#
#     make all      # rebuilds everything, offline, byte-identical
#
# The only network dependency is re-running the NVD joins against the LIVE API
# after deleting the caches (drift_cache.json / ve_cache.json); that path uses
# urllib from the standard library, still no third-party packages.
#
# The upstream corpus-fetch and Security-Policy-PDF extraction stage is NOT shipped
# here (the fetched corpus it produced is provided under corpus140_3/records/).
# That stage used:
#     pdfplumber
#     beautifulsoup4
